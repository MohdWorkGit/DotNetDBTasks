services:
  oracle:
    image: gvenzl/oracle-xe:21-slim
    container_name: dotnetdbtasks-oracle
    environment:
      ORACLE_PASSWORD: "${DB_PASSWORD:-YourStrong@Passw0rd}"
    volumes:
      - oracle_data:/opt/oracle/oradata
    healthcheck:
      test: ["CMD", "healthcheck.sh"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 90s
    networks:
      - dotnetdbtasks-network
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: docker/api.Dockerfile
    container_name: dotnetdbtasks-api
    environment:
      ConnectionStrings__DefaultConnection: "User Id=system;Password=${DB_PASSWORD:-YourStrong@Passw0rd};Data Source=oracle:1521/XEPDB1"
      Jwt__Secret: "${JWT_SECRET:-CHANGE_THIS_TO_A_SECURE_SECRET_KEY_AT_LEAST_32_CHARS_LONG_12345}"
      Jwt__Issuer: "${JWT_ISSUER:-DotNetDBTasks}"
      Jwt__Audience: "${JWT_AUDIENCE:-DotNetDBTasks}"
      Ldap__Host: "ldap"
      Ldap__Port: "389"
      Ldap__BaseDn: "dc=dotnetdbtasks,dc=local"
      Ldap__UsersDn: "ou=users,dc=dotnetdbtasks,dc=local"
      Ldap__AdminDn: "cn=admin,dc=dotnetdbtasks,dc=local"
      Ldap__AdminPassword: "${LDAP_ADMIN_PASSWORD:-LdapAdmin@123}"
      Ldap__UsernameAttribute: "uid"
      Ldap__DepartmentAttribute: "departmentNumber"
      Ldap__UserObjectFilter: "(objectClass=inetOrgPerson)"
      Cors__AllowedOrigins__0: "${APP_DOMAIN:-http://localhost}"
      Cors__AllowedOrigins__1: "http://client"
      ASPNETCORE_ENVIRONMENT: "Production"
    depends_on:
      oracle:
        condition: service_healthy
      ldap:
        condition: service_started
    networks:
      - dotnetdbtasks-network
    restart: unless-stopped

  ldap:
    image: osixia/openldap:1.5.0
    container_name: dotnetdbtasks-ldap
    environment:
      LDAP_ORGANISATION: "DotNetDBTasks"
      LDAP_DOMAIN: "dotnetdbtasks.local"
      LDAP_ADMIN_PASSWORD: "${LDAP_ADMIN_PASSWORD:-LdapAdmin@123}"
    volumes:
      - ldap_data:/var/lib/ldap
      - ldap_config:/etc/ldap/slapd.d
      - ./docker/ldap/bootstrap.ldif:/container/service/slapd/assets/config/bootstrap/ldif/custom/50-bootstrap.ldif
    command: --copy-service
    networks:
      - dotnetdbtasks-network
    restart: unless-stopped

  client:
    build:
      context: .
      dockerfile: docker/client.Dockerfile
    container_name: dotnetdbtasks-client
    ports:
      - "${CLIENT_PORT:-80}:80"
    depends_on:
      - api
    networks:
      - dotnetdbtasks-network
    restart: unless-stopped
    labels:
      - homepage.group=Manage
      - homepage.name=DBtasks
      - homepage.icon=oracle.png
      - homepage.href=https://dbtasks.alharthy.uk/
      - homepage.description=Manage DB queries

volumes:
  oracle_data:
    driver: local
  ldap_data:
    driver: local
  ldap_config:
    driver: local

networks:
  dotnetdbtasks-network:
    driver: bridge
